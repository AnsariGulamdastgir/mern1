services:
  - type: static
    name: car-scout-frontend
    env: static
    buildCommand: npm run build
    publishPath: ./dist
    routes:
      - source: /assets/*
        destination: /assets/*
      - source: /*
        destination: /index.html
